{
  "name": "eagle-eye",
  "version": "0.1.0",
  "description": "OmniRecon — a mobile-optimized geospatial command center PWA",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:frontend": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "test": "vitest run",
    "test:unit": "vitest run --reporter=verbose",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@eslint/js": "^9.21.0",
    "eslint": "^9.21.0",
    "globals": "^17.4.0",
    "jsdom": "^28.1.0",
    "vite": "^6.2.0",
    "vitest": "^3.0.7"
  }
}
