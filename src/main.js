/**
 * OmniRecon — main entry point
 * Geospatial OSINT command center PWA
 */

const app = document.getElementById('app');

if (app) {
  app.innerHTML = '<h1>OmniRecon</h1><p>Loading geospatial command center…</p>';
}
